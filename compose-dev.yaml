services:
  app:
    entrypoint:
    - sleep
    - infinity
    image: edgarknapp/net7dockerdevenv:latest
    init: true
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock

